export default function HomePage() {
  return (
    <main>
      <section className="mx-auto max-w-container-wide px-gutter py-24">
        <h1 className="max-w-2xl font-display text-h1 font-bold tracking-heading text-ink-primary">
          Connecting strength communities to the athletes who belong in them.
        </h1>
        <p className="mt-6 max-w-xl font-text text-body-lg text-ink-body">
          Organisers run events and competitions end-to-end in weightlifting,
          powerlifting, Hyrox, CrossFit and fitness Racing.
        </p>
        {/* TODO: HeroFlipbook, CTAs, overlay cards, and remaining Home
            sections — build sequence steps 3+ (separate review). */}
      </section>
    </main>
  );
}
