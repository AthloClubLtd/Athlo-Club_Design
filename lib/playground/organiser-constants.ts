/** The demo club the Organiser view acts as — one of the seeded public
 * clubs, so events it creates behave identically to seed data everywhere
 * else (Discover, follow, detail screens). */
export const DEMO_ORGANISER_CLUB_ID = "central-staffs-crossfit";
